"""OCR engine: extracts text from problem images using Groq vision or Tesseract."""

from groq import Groq


def extract_text(image_base64: str) -> str:
    """Send image to Groq vision model and return extracted text."""
    return ''
